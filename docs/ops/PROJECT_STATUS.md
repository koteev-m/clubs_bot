# Project status checkpoint

## Scope and validity

Repository: `koteev-m/clubs_bot` (`/Users/maksimmartynov/IdeaProjects/clubs_bot`). Current task: CLB-91 — Stage Recovery — Corrected Read-only Inspect & Root-Binding Reconciliation, continued with a local corrected-side prior API diagnostic patch. Earlier task entries below are historical evidence, not active execution instructions. This checkpoint is not a product specification, external ChatGPT journal, deployment record, or runtime migration checkpoint.

## Current CLB-91 continuation: local prior API phase diagnostics

- Existing **CLB-91 — Stage Recovery — Corrected Read-only Inspect & Root-Binding Reconciliation**; no new card or Goal, branch/worktree or subagents. Scope: local corrected-executor diagnostic adapter, its regression tests, release runbook and this checkpoint. Operational IDs `PSL-AC-29/34/35/36`; no blocking product decision or change to DEC-036/037/038, product requirements or accepted authority.
- Preflight: `main`, HEAD/local main/cached origin/main/fresh remote main all `b8bcd3029f9397963be5d2b92839b5e0f132933a`, tree `f3056deeb43a96794168f5c8c612a3d5e2c16c78`; one worktree, initially clean index/worktree, no untracked files or active Git operation. Fresh main used read-only `git ls-remote`; initial sandbox DNS failure was resolved with ordinary network permission. The older CLB-90 checkpoint was historical and did not override this Git evidence.
- Confirmed user handoff: usable v3 prior `34510160767:1:b8bcd3029f9397963be5d2b92839b5e0f132933a`; corrected run `34514548987` failed in validate, before Environment gate and SSH, with `PRIOR_STATUS_UNAVAILABLE`. Actions/Contents/Metadata read permissions and nonempty masked `GITHUB_TOKEN` were present; the local production verifier completed all nine reads. Exact hosted endpoint/HTTP status/transport root cause is unresolved. These hosted facts are supplied evidence, not a fresh API/hosted diagnostic check by this continuation.
- `CLB82_APPROVED_IMPLEMENTATION` is already provisioned; `CLB82_AUTHORIZED_ROOT_BINDING` remains absent. Neither was read or changed here. New instrumentation is for a future separately authorized hosted pass; it does not provide inspect, dispatch or recovery authority. No new inspect dispatch occurred.
- Adapter covers both `--verify-prior` and ordinary execution before helper/SSH. Fixed nine-phase allowlist and four failure classes emit only `corrected-prior-api:v=1 phase=<fixed_phase> failure=<fixed_failure>`. No success marker, raw API diagnostics, HTTP status, stderr/body/token/header/URL/path/argv output. Unknown contract fails before capture; nonzero results still yield `PRIOR_STATUS_UNAVAILABLE`, actual spawn OSError is re-raised unchanged to terminal `LOCAL_FAILURE`, and internal exceptions remain distinct. No retry/fallback, timeout/limit/endpoint/credentials/permissions changes.
- Authenticated producer chain, including `release_authority.py`, all workflows and approved helper identity/bytes remain byte-for-byte unchanged against HEAD and the preflight manifest. Authority blob `6100687ac37bd9cd96d2fe26269c0336dbcb72dd`, SHA-256 `83c8c81adeffc0ba7bb97d499ac3d8077950593ab03875856e17feccc8540216`; helper blob `fc09080ba4864133ca23ec5c777339b881094279`, size `174422`, SHA-256 `48bcbafde22b90dc3902cac0ba80754239964466ce612d11565dd1fdeb75e2ec`. Existing usable v3 producer-source compatibility is preserved.
- Result: `CLB_91_HOSTED_PRIOR_PHASE_DIAGNOSTICS_LOCAL_READY`. Final new diagnostic tests 7/7 PASS (8.913 s); focused prior/v3/security set 19/19 PASS (100.862 s); CLI authentication/replay/one-use set 10/10 PASS (227.160 s), with overlap: 23 distinct methods, zero final failures/errors/skips. All 61 old method ASTs are unchanged; no assertion or security predicate was removed. Final production bytes are the same across these runs; the final diagnostic-only umask cleanup was followed by the final 7-method rerun. Tests prove the real verifier argv/adapter and synthetic transport contract, not hosted HTTP/TLS/token behavior. Full 70+ minute corrected suite and separate unchanged read-only/helper suites were not rerun.
- Required `./gradlew test` PASS (4 s; 31 tasks up-to-date); `./gradlew detekt ktlintCheck` PASS (10 s; 14 executed/90 up-to-date); `./gradlew test -PrunIT=true` PASS (1m 27s; 2 executed/4 from cache/28 up-to-date), including local PostgreSQL/process-restart execution. Gradle results retain their cache boundary and existing nonblocking configuration-cache/deprecation warnings. Corrected-stage/full deployment/YAML/capability validators PASS (22 workflows), Python compile/syntax PASS, all four relative operational Markdown link targets exist, full diff/whitespace PASS. Final main/HEAD/cached upstream remain at the preflight SHA, clean index, exactly four intended unstaged files, no untracked fixture residue or active/unfinished operation.
- Initial diagnostic fixture error: invalid executable content with inherited PATH did not guarantee a spawn error; replaced with a missing executable in an isolated PATH, with no fallback. Required Gradle sandbox cache-lock failure was resolved through ordinary cache access; no gate was weakened.
- Evidence directory: `/private/tmp/clb91-prior-diagnostics/`. Only four intended repository files are edited, unstaged/uncommitted; test resources/caches and task-local evidence are local side effects. Dispatches = 0; GitHub external mutations = 0; runtime/server mutations = 0; publication = 0. No pin/secret/policy/settings/auth/permission mutation, SSH/provider/stage Docker/DB/migration/recovery or Hookah HT-13 action is authorized or performed.
- One next step: ChatGPT review of the local diagnostic patch before publication.

## Task history: CLB-76–80

- `CLB-76` — `COMPLETE / PR_READY`: PR #495 was made Ready for Review. This is inherited task history, not an action performed by CLB-78.
- `CLB-77` — `COMPLETE / PR_MERGED_CLEAN`: PR #495 was merged; the reported local `main` fast-forward and feature-branch cleanup are inherited history, not actions performed by CLB-78.
- `CLB-78` — instruction and checkpoint alignment. Scope: minimally align repository instructions for ChatGPT/Codex collaboration and record the current repository boundary. No Goal is created for this bounded documentation task.
- `CLB-79` — completed according to the CLB-81 user handoff; its audit/integration work is not repeated here.
- `CLB-80` — completed according to the user handoff: PR #496 merged, local main fast-forwarded, feature branch removed, and independent ChatGPT post-merge CI 15/15 success. Local Git now corroborates merge HEAD `18606bed36bdda2c8024b0f93f6c01d16ccabac4`; CI is inherited evidence, not rerun by CLB-81.

## Historical CLB-78 repository state and evidence

- Local inspection for CLB-78 found branch `main`, one worktree at `/Users/maksimmartynov/IdeaProjects/clubs_bot`, and `HEAD` `25c4cb06f46817d51d1773a1338f2afeb5e58f19`.
- Remote `refs/heads/main` of `koteev-m/clubs_bot` was independently read through `git ls-remote` and GitHub API as `25c4cb06f46817d51d1773a1338f2afeb5e58f19` on 2026-09-07. This does not assert the state of local cached `refs/remotes/origin/main`; that cached ref was separately read by `git rev-parse refs/remotes/origin/main` as the same SHA during CLB-78. This is merge PR #495, whose tree is `22822a6fff6cf15b042b780947eb7573f3a3e6bb` and ordered parents are `da9717cef94daacccc9ba998f9af30d0fe27f656`, then `bf2706fefabc9d22b4d4cf92f93d835e65f90a73`.
- Before CLB-78 edits, index and worktree were clean; no untracked files or active merge/rebase/cherry-pick/revert operation were found. CLB-78 intentionally leaves an uncommitted documentation diff, so this checkpoint does not claim a clean worktree after its edits.
- The CLB-77 report recorded the initial post-merge snapshot: 15 `push` runs, with 3 `success` and 12 `in_progress`.
- A later read-only ChatGPT check for exact merge SHA `25c4cb06f46817d51d1773a1338f2afeb5e58f19` found `total_count=15` for `event=push` and `total_count=15` for `event=push&status=success`; the subsequently confirmed result is therefore 15/15 success.
- These historical checks were not run by CLB-78 and do not verify deployment or runtime. Sources: [Clubs Bot — журнал проекта и точка продолжения](https://docs.google.com/document/d/1I4m-kQvDjQ6xTNXh5t6pcdtrpNn9dGSVSgOGuWwwtQ4/edit) and related [PR #495](https://github.com/koteev-m/clubs_bot/pull/495); CLB-78 did not recheck either link.

## Historical CLB-78 operational boundary

- Collaboration, authority, task-record, model-selection, verification and reporting rules are maintained compactly in root `AGENTS.md`; product truth, accepted decisions and security/release requirements remain authoritative under their existing documents.
- The release selector and CI fixture correction is in `main`; it does not prove deployment or a restored staging runtime. Retained one-off migration-container diagnosis remains historical evidence only.
- Runtime recovery, out-of-band host-key authentication, `SSH_KNOWN_HOSTS` provisioning, Release Status dispatch, resume and recovery require separate authority and are outside CLB-78.

## Historical CLB-78 operations, blockers and next step

- Completed CLB-78 checks: local Git preflight; applicable instruction and `CONTRIBUTING.md`/product-readme reading; scoped working-memory search; remote-main verification; `git diff --check`; full review of the tracked diff and untracked checkpoint; whitespace and checkpoint-link checks. No project documentation validator applies to these two Markdown files; the available validators target deployment, Mini App, payments or workflow YAML.
- CLB-78 prepared the local instruction/checkpoint edit, received text review, and applied the checkpoint-discipline, remote-reference distinction and CI-provenance clarifications. The diff remains uncommitted and is not final ChatGPT acceptance.
- No deployment, staging-runtime confirmation or release-status workflow execution has been performed. Their evidence is unverified here, and no recovery authority exists in this task.
- Allowed for CLB-78: minimal local edits to applicable instructions and this checkpoint, followed by documentation-diff validation. Prohibited: product/code/test/workflow/deploy/config changes; Git publication operations; external writes; SSH/provider/database access; dispatch, deploy, migration, resume, rollback or recovery.
- Next step: review the current unstaged CLB-78 diff and transmitted texts in ChatGPT before any publication.

## Historical CLB-81: Stage Recovery — Incident Reconciliation & Exact Resume Plan

- Result: `CLB_81_PREPARATION_COMPLETE_WITH_BLOCKERS`. New card, no new Goal or worktree. Local branch `main`, HEAD `18606bed36bdda2c8024b0f93f6c01d16ccabac4`, one worktree; initially clean with no conflicting edits or active Git integration operation. Only this checkpoint is edited and remains uncommitted.
- Scope: incident preparation under root `AGENTS.md`, the release runbook and roadmap execution gates. Operational evidence supports `PSL-AC-29`, `PSL-AC-34`, `PSL-AC-35`, `PSL-AC-36`; no product requirement or accepted decision changes. `DEC-036`/`DEC-037` remain accepted; unresolved execution prerequisites are not new product decisions.
- Read-only SSH observer snapshot at 2026-09-07 06:26:01–06:26:02 UTC, followed by a matching persistent/base-Compose override check: `/opt/clubs-bot-stage`, project `clubs-bot-stage`, services `app`, `db`; owner `33468965282-1`, environment `stage`, revision `44497dcd28139cef865c3f98ac3f2c4a5afac636`, digest `ghcr.io/koteev-m/clubs_bot/app-bot@sha256:ddf5486e02835855178cc3b30bd2f22899335131e6dc388def20feac328016fe`. These are incident identities, not main/helper revision.
- State root is `/opt/clubs-bot-stage/.clubs-bot-release-state/stage`. Active checkpoint remains `migration_completed`; result `33468965282-1.result` records operation `start`, before/after `migration_completed`, `remote_failure`, `app_identity_mismatch`. Ledger is version 1/completed; outcome is version 1/succeeded with `migration_succeeded`. Exact identity and invocation correlation recompute successfully: `f8558094d562e0693cfa2d59d8b25b3ff09c4da505dd87eb6be25990db243762`. Historical `applied=4` was not independently recounted through SQL or logs.
- Retained `clubs-bot-migrate-33468965282-1` is exited, exit 0, project/service `clubs-bot-stage/app`, oneoff `True`, and matches candidate image reference/image ID. Candidate image revision matches the incident. No ordinary app container; DB running/healthy. Effective app port remains `127.0.0.1:18084 -> 8080`. Persistent and state overrides match recorded SHA-256 `8fd0020672029f430ab3e3d1dfd67ff434f6b11e853c5445a6d6e9f8997ada59`; base Compose has exactly one expected candidate image.
- Both existing lock files admitted nonblocking shared read locks during inspection. No additional active/finalizing state was observed; GitHub deploy queued/waiting/pending/in-progress counts are zero. This is a bounded observation, not permanent exclusion of another operator. No canonical helper status was executed as root.
- Retained helper `/tmp/clubs-bot-release-33468965282-1.sh`: regular non-symlink, one link, 138142 bytes, UID/GID 1000, mode 0644, SHA-256 `8d8321d325d6ca25f48bcfdd7d9fb0eeb6f80af9c26f136ea06953cf1c2b914e`; matches raw incident Git blob. Shared/state directories and records belong to UID 1000 with 0700/0600 modes. Observed ext4 backing fingerprints match binding version 3/mount version 2. Root observer is not evidence of deployment-principal identity or trusted status.
- Corrected helper at current HEAD: Git blob `d4741ff04e0ffc578157c607b885333464c251d5`, SHA-256 `5503333309d30a4401f8712b259b31cc4085b6793c8ac418453f23bff16188b1`; implementation commit `3ac426e0da6326795c4bf6d1de228b891652f3d9`, merged through #495. Incident-to-main diff changes only ordinary-app selection; binding/checkpoint/result/ledger/outcome formats are unchanged. Same-owner/revision/digest `resume start` is structurally compatible, subject to authoritative guards.
- GitHub read-only metadata: incident run `33468965282`, attempt 1, completed/failure; exact tag `deploy-stage-44497dc` resolves to incident revision. Stage matches DEC-037 reviewer/self-review/wait/bypass/main-only policy. Repository secrets: 0; stage secrets: 5; overlap: 0; `SSH_KNOWN_HOSTS` absent in both. Release Status runs: 0. No secret values were read.
- Blockers: independent host-key authentication/fingerprint evidence absent; pinned stage secret absent; deployment-principal trusted status absent; mutation authorization absent. Existing local known_hosts provided continuity only. These block execution, not the completed static preparation.
- Selected technical path for future review: preserve incident identity, ledger/outcome/container and retained helper; observe original incident through Release Status first, then execute a separately verified corrected implementation snapshot under the actual deployment principal, perform corrected read-only status, and only then exact `resume start`. The repository has no reviewed runner/procedure that binds those corrected execution bytes and principal to this existing incident while preserving the old status channel. Ordinary `quiesced-release.sh` uploads then runs the full release and cannot serve this purpose; Release Status intentionally verifies/executes incident bytes only. No new CLI or implementation is introduced here.
- Minimum missing mechanism: a bounded deployment-principal executor/procedure with pinned implementation Git-blob SHA-256, same verified snapshot for status/resume/reconciliation, exact incident inputs, protected-environment/concurrency/trust gates, fail-closed principal/path/state guards, preserved old helper, explicit one-shot authorization and acknowledgement-loss handling. It must not rerun migration, replace owner, start old image, repeat start from `candidate_start_begun`, or combine cleanup with start. The CLB-81 report specifies exact existing helper arguments and expected transitions; deployment transport/principal and this mechanism remain unproven.
- Checks: scoped source/diff/test-contract inspection; unchanged helper/test/CI artifacts since #495 and inherited successful post-merge CI reused; Bash syntax checks passed. Initial sandbox GitHub/SSH network failures were resolved by standard environment escalation. A local observer allowlist initially omitted `requested_operation` and stopped safely; corrected read-only inspection passed. An incorrect GitHub workflow filename returned 404; actual `deploy-ssh.yml` queries succeeded. No Gradle or full release suite was run; no general Markdown validator applies. Checkpoint diff/whitespace reviewed separately.
- Effects and permissions: only this local checkpoint changes. Read-only GitHub metadata and scoped SSH file/stat/hash/findmnt/Docker/Compose inspection, including transient shared locks; ordinary access/audit metadata may be produced by reads. No helper execution/upload/replacement, remote file writes, lifecycle, SQL/Redis/Flyway, secrets provisioning, dispatch, deployment, resume, recovery, cleanup, Git publication or global configuration changes. Historical product runtime claims remain dated snapshots, not current app availability. No unfinished remote mutation is claimed or authorized.
- One next step: separately authenticate the SSH host key for `178.20.209.5:22` through the independent provider/VNC control plane and verify its fingerprint for this exact host, closing the first unmet execution gate; do not provision secrets or dispatch as part of that step.

## Historical CLB-82 initial implementation: Stage Recovery — Corrected Helper Executor & Regression Tests

- Result: `CLB_82_EXECUTOR_LOCAL_DIFF_READY`. New card; no Goal, branch, commit, PR or worktree created. Starting and final local Git: `main`, HEAD `18606bed36bdda2c8024b0f93f6c01d16ccabac4`, one worktree, index clean. Expected inherited CLB-81 checkpoint delta `+25/-5` was read and preserved before CLB-82 additions; its evidence remains historical preparation, not execution. All 13 changed/new files remain unstaged/untracked for independent review.
- Scope under root `AGENTS.md`, `CONTRIBUTING.md`, release runbook and roadmap gates: repository-local executor, tests, necessary workflow/security-validator/quality-fixture wiring and operational documentation. Relevant operational acceptance IDs: `PSL-AC-29`, `PSL-AC-34`, `PSL-AC-35`, `PSL-AC-36`; no unresolved product decision blocks this implementation, no product/decision/source changes. `DEC-036`/`DEC-037` and the live execution gate order remain authoritative.
- Added separate manual stage workflow and bounded Python runner with inspect default, exact incident/implementation validation before secrets, protected deployment principal, strict pinned host trust, `payments-schema-stage` concurrency, exact-action/run-number authorization and rerun refusal. The helper is captured once from the pinned Git object and reused as immutable bytes for status, one resume start and one read-only resume-start reconciliation. Existing helper argv, release state formats, migration semantics and retained helper are unchanged. Shared pure temporary-root validator and canonical status grammar preserve old read-only behavior; old workflow still derives the incident helper hash.
- Local identity proof reconfirmed: incident-to-main helper diff changes only `current_compose_container_id()`; main source path mode `100644`, blob `d4741ff04e0ffc578157c607b885333464c251d5`, SHA-256 `5503333309d30a4401f8712b259b31cc4085b6793c8ac418453f23bff16188b1`, size `138833`. These are implementation identities, not the incident revision or actual principal identity.
- Verification: `./scripts/selfcheck-quality-gates.sh` passed with final `selfcheck: OK`; new corrected executor 22/22, old Release Status 80 methods/322 subtests, release-state 73/73, with no failures/errors/skips. Actual helper fixtures cover preserved migration/container/records, one start, competing locks, pre/post-start drift and acknowledgement loss. Syntax, workflow YAML/capability validators (22 visible workflows), quiesced-deployment validator, local documentation links and full diff/whitespace checks passed. Fresh `./gradlew --no-daemon --no-configuration-cache --no-build-cache --rerun-tasks --continue test detekt ktlintCheck` passed (112 executed tasks); `./gradlew --no-daemon --no-configuration-cache --no-build-cache test -PrunIT=true` passed (6 executed, 28 up-to-date), including local Testcontainers PostgreSQL.
- Initial verification failures: sandbox denied the Gradle cache lock; standard test escalation resolved it. A cache-enabled run restored stale CLB-19 ktlint output, so that evidence was rejected and unit/static checks rerun fresh. Fixture environment, extracted-validator locations and workflow inventory were corrected without weakening gates; final applicable suites passed. Evidence logs and the complete combined HEAD-to-local diff, including retained CLB-81 and new files without staging, are in `/private/tmp/clb82-evidence/`. No unfinished test or remote operation remains.
- Execution remains prohibited in this task. Independent host authentication, pinned stage `SSH_KNOWN_HOSTS`, independently verified protection/no-overlap metadata, original deployment-principal trusted Release Status result, separate corrected inspect permission and exact one-shot mutation authorization remain future gates. No actual SSH/VPS/provider/browser calls, secret-value reads/provisioning, workflow dispatch/rerun/cancel, deployment, server lifecycle/migration/recovery/cleanup, Git publication or global configuration changes occurred. Only synthetic local test resources and standard test caches may be written.
- One next step: independent review of the completed local implementation and full unstaged/untracked diff.


## Historical CLB-82 continuation: F1/F2 authority repair after first CLB-83

- Same card `CLB-82 — Stage Recovery — Corrected Helper Executor & Regression Tests`; no new card, Goal, subagent or worktree. Independent CLB-83 reviewed the prior local snapshot and found two P1 implementation defects: F1 lacked verified original Release Status evidence; F2 allowed another process to reuse authorization after an unacknowledged submission before a new remote result. This supersedes the initial readiness claim for review; no final acceptance or stage execution is asserted.
- Initial snapshot independently matched: `main`, HEAD `18606bed36bdda2c8024b0f93f6c01d16ccabac4`, one worktree, clean index, 13 modified/new files (six untracked), `+1182/-59`; full patch SHA-256 `091d14cfc64121be3f7dd5d9a054172fde8c333604ea144dd382b75dfee0f82c`; reviewer manifest `1bb42c4c9353a6179b3923e21c9a64f50dffe60bca2f1c6fcbbf0419fd6c0e96`; corrected runner `218ddf6afb650dc8a530b6fa07eb764319262f4e2c23acb1f9314d5442991040`. No foreign edits were found. CLB-81 evidence and inherited `+25/-5` remain preserved; original CLB-82/83 evidence files are untouched.
- Before edits, copied reviewer probes reproduced F1 (`--validate` returned 0/VALIDATED without prior evidence) and F2 (two independent CLI processes submitted twice; actual-helper probe showed starts 0→1, migrations=1). Copies redirected only evidence/fixture paths. New command/cwd/exit/hash records and logs are kept separately in `/private/tmp/clb82-authority-fix-5w73xxju/`.
- F1 repair: mandatory `prior_status=run_id:attempt:head_sha`; offline validation is explicitly distinct from verification. Main CLI reads fixed GitHub API endpoints, authenticates original workflow/revision/attempt/job and producer bytes, and checks one bounded canonical trusted result with exact incident/operation/principal provenance. Protected original status job emits only safe provenance after the existing SSH validation; retained incident helper hash and read-only server behavior remain intact. `actions: read` with the existing workflow token is the only added GitHub permission; no PAT/service or API writes. Old selector `resume_permitted=no` is accepted; corrected readiness and mutation authorization remain separate.
- F2 repair: a distinct fixed consumed record in the existing persistent stage root, atomic no-follow `O_EXCL` creation, mode 0600 and file/parent fsync before any resume submission. All processes/jobs/runners share this remote incident namespace. Existing/partial/malformed entries deny reuse; missing roots are never recreated, uncertain consumption denies resume, and cleanup does not remove the record. Consumed authority, attempted submission and durable successful start are distinct evidence. No migration/binding/checkpoint/result/ledger/outcome format or helper-byte change.
- Result: `CLB_82_AUTHORITY_FIX_LOCAL_DIFF_READY`, for focused CLB-83 re-review, not final acceptance. Scope remains `PSL-AC-29`, `PSL-AC-34`, `PSL-AC-35`, `PSL-AC-36`, under root AGENTS/CONTRIBUTING and roadmap gates; no blocking product decision or product-document change. Git remains `main` at the same HEAD, one worktree, clean index; 15 modified/new paths (seven untracked), of which 11 belong to this repair. All changes remain local.
- New verification: `./scripts/selfcheck-quality-gates.sh --ci-delegated-release-state` returned 0 / `selfcheck: OK`, including 31/31 executor tests (nine F1/F2 methods plus the retained 22), old Release Status 80 methods/322 subtests, strict diagnostic fixtures and the exact delegated release-state CI path. Final GitHub-environment isolation changes were separately checked through production CLI positive/negative F1 cases and a fresh F2 replay; syntax, YAML, workflow capability and quiesced-deployment validators returned 0. Commands, cwd, exit codes, before/after source hashes, phase hashes and logs are in the new evidence directory. No failed diagnostic or earlier snapshot is presented as a fresh final PASS.
- F2 measured result with valid F1 evidence and the actual helper: two independent CLI processes after SSH 255 before a new remote result give claim attempts=2, durable claims=1, resume submissions=1, starts=0, migrations=1; both exit 1, and the second stops at consumed/unknown authority. Concurrent successful callers give claims=1, submissions=1, starts=1, migrations=1. First successful execution preserves retained helper/container and ledger/outcome, checks `/ready` and `/health`, and prevents restart. Interruption after consumption, lost claim acknowledgement, write/fsync failure, changed run/temp context, missing storage and partial/malformed/unsafe entries deny submission.
- Verification failures were classified and resolved: the initial sandbox denied the Gradle cache lock, then standard local-test escalation succeeded; stage fixture migration-log cleanup required a narrowly scoped synthetic-owner allowance; the fake findmnt counter required atomic bookkeeping for concurrent status, without changing helper locks; an unsolicited loser status could contend with the winner and was removed. Producer failure injection was corrected to use the harness transport-failure mode. Corresponding regressions passed; no production guard or validator was weakened.
- Reused evidence is explicitly separate: prior CLB-82 fresh Gradle test/detekt/ktlint (112 executed), IT (6 executed/28 up-to-date, local PostgreSQL), and release-state 73/73 PASS at the same HEAD. `reused-evidence.json` records log SHA-256 and 1,111 unchanged HEAD file hashes, including backend/Gradle and helper/release-state sources; these unchanged suites were not repeated solely for a new report. Changed status/executor paths and their actual-helper interactions were rerun. Full and repair-only patches include untracked files without staging; CLB-81 and the initial CLB-82 body are preserved, and reverse-check/whitespace verification passes.
- Execution permissions remain local edits and isolated tests only. Independent host authentication, protected-environment metadata, pinned host key, actual original status run and separate exact execution authorization are still future gates. No real SSH/VPS/provider, secret-value read/provisioning, workflow dispatch/rerun/cancel, server Docker/DB/lifecycle or remote writes; no stage/add, commit, push, PR, branch/worktree/global-config change. Synthetic fixtures and their own cleanup are the only operational effects.
- One next step: continue CLB-83 for focused re-review of F1/F2 and the repair-only/full diff.

## Historical CLB-82 continuation: imports and root continuity after repeated CLB-83

- Same card, no new Goal/subagent/worktree. Root AGENTS.md, CONTRIBUTING.md, scoped roadmap/AS_BUILT/gap evidence and release runbook apply; operational IDs remain `PSL-AC-29`, `PSL-AC-34`, `PSL-AC-35`, `PSL-AC-36`, accepted `DEC-036`/`DEC-037`; no new product decision or product implementation. No additional skill applies.
- Re-review snapshot matched all 15 files (seven untracked), `+1897/-59`, clean index, one worktree, `main` / `18606bed36bdda2c8024b0f93f6c01d16ccabac4`; full patch `4ed0da86e9fe4b882448258c68c27c4391720ae3313785b40dde9d77d289afec`, canonical reviewer manifest `4987e300f5e257cac442b43067ed73e1a93ea9d762cc21ac381749b7a552900d`, runner `977e2d7855e346b8710f64d72c4559fe6aefbd9244d6c24be46263c90baae10c`. CLB-81 inherited evidence and all previous CLB-82/83 materials are preserved. New evidence only: `/private/tmp/clb82-import-root-fix-uzla8_pq/`.
- Before edits, copied reviewer probes reproduced all three defects with all 15 source hashes unchanged: forged producer owner accepted; remote Python cwd import returned two ACKs with zero durable claims and two resumes; root replacement produced two durable claims on different directories and two resumes. Both latter actual-helper probes had one start and one prior migration. Exit 0 of these drivers means reproduction, not repair.
- Import repair: all affected production Python invocations use `-I -S -B` before startup/imports; exact own source files are compiled without checkout `sys.path` or `.pyc`. Full five-file producer closure remains verified, checkout is exact workflow SHA, provenance v2 rejects the vulnerable producer/v1. Original incident helper/hash and canonical prior-status/incident/attempt/job/principal contract remain intact, including old selector `resume_permitted=no`. Fixtures cover cwd, PYTHONPATH, user site/startup, PYTHONHOME, unchecked dependency bytecode, symlink entrypoint neighbors and full dependency source mismatch; no poison is installed outside disposable fixtures.
- Result boundary: `BLOCKED_ROOT_BOUND_RESUME_CONTRACT`. The existing claim and resume still have the reviewed root substitution window; this is not an arbitrary storage rollback. The pinned helper accepts neither expected root identity nor inherited lock handoff, reopens state/locks by pathname, and an externally held exclusive lock prevents its nonblocking acquire. Releasing it or adding another stat restores a gap; advisory locks cannot prohibit rename. Safe continuity requires an explicitly scoped helper/state-access contract change or filesystem namespace isolation beyond current pinned-byte/access constraints. No such extension, fake approval flag, helper-byte edit or state-format change was made.
- Existing status fault injection used PYTHONPATH/sitecustomize and therefore became correctly inert under isolation. Its fixture now prepends the same fault hooks explicitly at a fake interpreter boundary, preserving production flags/argv, all assertions and the strict 80-method/322-subtest contract. This diagnostic failure requires a new selfcheck on the corrected fixture; the earlier run is not a PASS.
- Mandatory executable root/parent regression has no expectedFailure/skip exemption and is intended to keep delivery blocked. Existing positive controls and F1/F2 tests remain; new tests distinguish claim attempts, durable markers/root identities, submissions, starts and migrations. Final commands, exit codes, hashes and full/repair patches are recorded with this continuation's report; a failing root gate must not be reported as suite PASS.
- Backend/Gradle/IT and pinned release-helper scopes remain unchanged; prior fresh Gradle test/detekt/ktlint and IT evidence may be reused only after rechecking their source hashes. Changed Python/status/executor/validator paths require new runs. No server execution, GitHub write/dispatch, secret access, Git publication/index/branch/worktree mutation or global environment change is authorized or performed. Independent host authentication, protected stage evidence, pinned SSH_KNOWN_HOSTS, trusted original deployment-principal status and separate execution authority remain future gates, additional to this implementation blocker.
- Final frozen-source verification: `./scripts/selfcheck-quality-gates.sh --ci-delegated-release-state` completed with exit 1 solely at the required root/parent regression. Old status strict suite passed 80 methods / 322 runtime subtests; all 31 inherited executor methods and five other new methods passed (37 methods total, one root method with two failing subtests). Each failing root variant had two durable claims on distinct inode identities, two submissions, one start and one retained migration. Positive actual-helper control retained one claim/resume/start/migration; remote poison replay retained one claim/submission and zero starts after 255. Syntax/workflow/security validators and focused Python 3.11 checks passed. The final selfcheck before/after hashes match; only this outcome paragraph was added afterwards, with a separate documentation/diff check. `final-results.json`, `verification-index.json` and full/repair patches in the evidence directory bind commands/cwd/exits to final executable hashes; diagnostics and reused Gradle/IT evidence are labelled separately.
- One next step: continue the same CLB-83 for focused re-review of the import repair, retained root regression and the exact missing root/lock contract.

## Historical CLB-82 continuation: authorized bound-state contract

- Same `CLB-82 — Stage Recovery — Corrected Helper Executor & Regression Tests`; no new Goal/card/subagents/worktree. Latest independent verdict `CLB_83_IMPORT_REVIEW_ROOT_CONTRACT_READY` closed both import findings and left root/parent P2 open. User explicitly authorized only local additive helper path, fixed incident root-binding pin, descriptor-relative state/configuration, helper-owned locks, durable claim before separate resume and associated wiring/tests/docs. The old helper-freeze blocker is historical; no publication, server access, pin provision or execution authority was granted.
- Starting snapshot matched all 15 working paths (eight modified/seven untracked), `+2261/-69`, `main` / `18606bed36bdda2c8024b0f93f6c01d16ccabac4`, one worktree, clean index. Full patch `826223b1c3f00884dbbf8b40b42daed5aeb892d218a680da685057854c4af38a`; canonical manifest `b9907396addd1fef871c0afe9d6095938ba0dbac0401e816d1ae8ac492f8f286`. All inherited CLB-81/82/83 edits/evidence preserved; baseline including untracked and new evidence are separate in `/private/tmp/clb82-bound-state-n4xzrc8g/`.
- Added `corrected-start OWNER stage COMPOSE REVISION IMAGE PHASE`, with inspect/claim/resume-start/reconcile and private FD4 control. Each phase opens pinned ancestors/child directories/lock files, checks existing binding/state/migration evidence, owns application→operation locks and uses descriptor-relative reads/writes/finalization. No legacy recursive resume, bypass flag, bootstrap, migration, rollback or cleanup. Existing record schemas/incident identities and legacy CLI remain compatible.
- Protected configuration supplies `CLB82_APPROVED_IMPLEMENTATION` and `CLB82_AUTHORIZED_ROOT_BINDING`; no dispatch override or production values provisioned. Pin includes exact incident/implementation/principal, Compose identity, persistent backing, stable object dev/inodes and captured configuration hashes. Inspect can expose candidate binding for a separate decision; never adopts it. Authorization includes exact implementation and canonical pin digest. Claim durably persists consumed record plus token hash before a separate resume; only acknowledged creator has its private token. Lost ACK/signal/255/restart/new run cannot reclaim. B is rejected, restored A retains marker; finalization uses held directories even if public names change.
- Configuration coverage includes checked main YAML/managed overrides/`.env`, restricted file-expansion surface, rendered Compose model, explicit project/relative-path semantics, empty captured Docker client configuration and same-context health checks. An offline real Compose probe exposed duplicate dollar escaping and macOS FD reopen differences; duplicate escaping was fixed. macOS fixtures adapt captured bytes at fake Docker boundary; Linux sealed-memfd/proc behavior is explicitly unverified locally. No Engine/server is contacted. Full helper regression evidence must be new because helper bytes changed; prior Gradle/static/IT evidence is eligible only for unchanged backend hashes.
- Applicable root AGENTS/CONTRIBUTING and scoped roadmap/AS_BUILT/GAP evidence: PSL-AC-29/34/35/36; DEC-036/037 unchanged, no new blocking product decision or product-scope audit. Remaining LIVE gates: independent host authentication, protected stage configuration/main policy, pinned SSH_KNOWN_HOSTS, original trusted deployment-principal Release Status provenance v2, reviewed real implementation Git identity/root pin, and separate exact one-use execution authority. Historical UID1000/root observer is not principal proof. Workflow, pin and recovery remain undeployed/unexecuted.
- Local candidate result: `CLB_82_BOUND_STATE_LOCAL_DIFF_READY`, for the same CLB-83 focused review; no final acceptance. Final `./scripts/selfcheck-quality-gates.sh --ci-delegated-release-state` returned 0 / `selfcheck: OK`: 49/49 executor methods, retained strict read-only status 80 methods/322 subtests, workflow/security/structure fixtures and delegated wiring. Full changed-helper `python3 -B scripts/tests/test_quiesced_release_state.py --strict-ci` freshly passed 73/73 with no skips/errors. Python 3.11 selected six import/durability/positive controls and focused three concurrency checks returned 0. Commands/cwd/exits and exact loaded source hashes are in `/private/tmp/clb82-bound-state-n4xzrc8g/CLB82-bound-final-verification.json` and `verification-index.json`.
- New uncommitted implementation identity: `scripts/deploy/remote-compose-release.sh`, blob/100644, 174422 bytes, Git blob `fc09080ba4864133ca23ec5c777339b881094279`, SHA-256 `48bcbafde22b90dc3902cac0ba80754239964466ce612d11565dd1fdeb75e2ec`. No future commit is invented; former implementation hash is historical. Full/incremental patches include untracked files without staging; initial 15-path snapshot and CLB-81 delta remain preserved. Nine paths changed in this continuation; the helper is the only additional working path (16 total, seven untracked).
- Actual-helper fixtures: root/parent/full-copy B before claim are rejected; restored A has one durable claim/submission/start and the same one migration. A copied marker between claim/resume does not authorize B; one claim/submission, zero starts. Lost pre-result submission ACK yields one claim/submission, zero starts across independent CLI processes. First success verifies exact candidate, /ready, /health and durable result; migration records/container and retained helper remain intact. All poison, wrong token/pin, nested/lock swaps, captured configuration, finalizer, signal and write/fsync negative cases remain executable without skip/xfail.
- Development failures remain recorded, not presented as final PASS: initial raw fixture migration-log cleanup rejected its synthetic log; Compose capture diagnostics exposed double dollar escaping/macOS FD reopen behavior; poison fault hooks and short-write transport injection needed test-boundary repairs. An earlier complete run had one initial `STATUS_UNAVAILABLE` (expected one durable claim, actual zero); its cause was not established and did not recur in 30 parallel inspect calls, so it remains recorded as a transient verification limitation. Separately, a late competing claim can safely hold the lock before the winner's separate resume (one consumed claim/submission, zero starts); positive concurrency scheduling and a separate mandatory busy/restart regression now cover both outcomes. A cumulative-counter assertion and two mistyped Python 3.11 test selectors were corrected and rerun. Final entrypoint review also reproduced a public candidate exposing the synthetic SSH username and one remaining Linux-incompatible id-fixture shebang. Candidate logs now carry principal name_sha256/UID, while private binding validation remains exact; literal output cannot be adopted as a pin. The id fixture uses env -S and its actual Linux-style argv is exercised. No production lock/guard or test acceptance criterion was relaxed.
- Gradle test/detekt/ktlint and isolated PostgreSQL IT evidence are explicitly reused after verifying 1110 unchanged backend/build artifacts; fresh release-state evidence replaces the old helper PASS. Target Linux memfd/proc/mount execution remains unverified locally: native macOS FD/flock/fsync fixtures and real offline Compose 5.1.1 are the stated alternative, with Linux-style interpreter argv separately exercised. No Engine/server was contacted. Only local sources, evidence and disposable fixtures changed; no publication/index/ref/worktree mutation, real SSH/API write, secret read, pin/Environment provisioning or server lifecycle occurred.
- One next step: return this candidate and its full/incremental diff to the existing CLB-83 for focused re-review. All LIVE gates above remain open; no workflow publication, pin provision or stage recovery is asserted.

## Historical CLB-82: mechanical EOF follow-up

- CLB-83 returned `REVIEW_PASS_LOCAL_CANDIDATE` for full patch `8ff072a383b79e01657f009480a97393eaf48c5994d388fe78194302f32b881c`, with no blocking P0-P2 findings. Independent Linux evidence covers 14 distinct methods (3 + 11), not a full-suite rerun; see [acceptance report](/private/tmp/clb83-linux-regression-3c64kl0y/CLB83-acceptance-report.md). Historical `STATUS_UNAVAILABLE` remains unexplained and was not classified as a blocking defect of the current contract.
- P3 EOF normalized: only the extra final LF in `scripts/deploy/release_private_root.py` was removed (1578 -> 1577 bytes), retaining one terminating LF, preceding bytes, UTF-8, regular type and mode 0644. Syntax, AST equality without locations and the existing private-root bootstrap test passed. This EOF/checkpoint delta is mechanical, not a new implementation or an already accepted ChatGPT delta.
- Historical scope blocker (resolved below): `BLOCKED_STATUS_VALIDATOR_PIN_SCOPE`. The unchanged validator pins the previous private-root SHA at `scripts/validate-quiesced-deployment.sh:800-801`; its status-channel check passed before normalization and now exits 1 / `shared status dependency content changed`. Updating that pin requires a third repository path outside this continuation's permission; no hash check was weakened or old SHA substituted. New evidence and manifests: `/private/tmp/clb82-eof-252xdufu/`.
- Pin reconciliation: `CLB_82_EOF_PIN_RECONCILED_LOCAL_READY`. User authorized only the validator/checkpoint follow-up; only the private-root expected SHA changed to the independently verified `250d359d114779ddcc12c38bc64dc7015d5679f0ffce12a859f007810726cdba`. The 1577-byte EOF-normalized dependency is unchanged. Bash syntax and status-channel validation passed; the disposable fixture accepted normalized bytes (exit 0) and rejected the former 1578-byte version (exit 1, exact content mismatch). `BLOCKED_STATUS_VALIDATOR_PIN_SCOPE` is resolved without changing guards, other pins or behavior. Prior AST/bootstrap evidence remains applicable; full suites were not rerun. New evidence: `/private/tmp/clb82-eof-pin-uo5f5e3x/`. This mechanical delta still awaits ChatGPT comparison with the accepted CLB-83 candidate.
- Same main/HEAD `18606bed36bdda2c8024b0f93f6c01d16ccabac4`, one worktree and clean index; no new card, Goal, subagents or worktree. Publication and LIVE execution were neither performed nor authorized; all existing execution gates remain. One next step: pass this mechanical delta and new manifest to ChatGPT for comparison with the accepted candidate, before a separate publication decision.

## Historical CLB-82: fixture portability correction

- Local preflight confirmed `codex/clb82-bound-state-executor`, HEAD/upstream `82b2ecba3a1e121448de3c82d2b81ef95c5b3cdd`, tree `12b1877e7e9869f16d1fde7c13790a4223e15c72`, one worktree, clean index/worktree and no active Git operation. PR #497 OPEN/Draft is the CLB-84 handoff, not a new API check. Same card, no new Goal/branch/worktree/subagents. Root AGENTS/CONTRIBUTING apply; operational PSL-AC-29/34/35/36 and accepted DEC-036/037 are unchanged.
- Hosted Lint run `34263538121`, attempt 1, job `102187142002` failed before any executor method: hardcoded `/private/tmp` was absent on Ubuntu. The retained local log and an actual Linux run of the original test bytes reproduced ENOENT with zero methods; no missing directory was created to bypass it.
- Test-only correction: select the existing `tempfile.gettempdir()` root, resolve aliases and reject checkout-contained roots before Git fixture creation/copying. Both disposable Git fixtures use this check. Three fresh-process regressions cover default/custom/symlink roots, absent macOS parent, checkout rejection, 0700 permissions, uniqueness and cleanup. Existing authoritative-state fixtures and all 49 earlier methods remain; no global tempfile setting changed.
- Final executable bytes passed 52/52 methods on macOS/Python 3.13.2 (4280.149 s) and Ubuntu 24.04 aarch64/Python 3.12.3 (1919.935 s), each with zero failures/errors/skips. `./scripts/selfcheck-quality-gates.sh --ci-delegated-release-state` passed (4999.990 s), including its own 52-method executor run and strict read-only status 80 methods/322 runtime subtests. Syntax, bound-structure, workflow-capability and status-channel validators passed.
- Preserved setup failures: the new cleanup test initially encountered Git's macOS `xcrun_db` cache; cleanup now verifies the owned fixture paths and outer directory. The first Linux full run had three subtest failures in the original producer method because unset RUNNER_TEMP selected world-writable `/tmp`; the existing guard correctly returned LOCAL_FAILURE before SSH. With the harness's supported RUNNER_TEMP `/work/runner-temp` (0700), that method and the full suite passed without code/guard changes or moving authoritative state. The first sandboxed selfcheck stopped on a Gradle lockfile permission; its approved rerun passed.
- Gradle test/detekt/ktlint and isolated IT evidence is reused after matching 1110 backend/build hashes; prior strict release-state 73/73 is reused after matching helper, legacy runner and suite bytes. These are not new runs. The opt-in live GHCR test remains unexecuted; hosted CI was not rerun. All runtime sources, pins, workflows, validators and security policy are unchanged; prior CLB-81/82/83 evidence and LIVE gates remain.
- Evidence: `/private/tmp/clb82-fixture-portability-0cpwmm33/` contains commands/cwd/exits, before/after source hashes, logs and the final diff/manifest. Linux used isolated containers without network, credentials or Docker socket; owned containers/volumes/image were removed. Only the test file and this checkpoint remain unstaged/uncommitted. No publication, PR/workflow action, server/DB access, provisioning or recovery was performed or authorized. Next step: review this local fixture correction before publication.

## Historical CLB-82: local Lint timeout correction

- Preflight: `codex/clb82-bound-state-executor`, HEAD/upstream `502c229abbef6fe4b6cfc90321383381c995a1c7`, one worktree, clean index/worktree, no active Git operation. Same card; no new Goal, branch, worktree or subagents.
- Confirmed hosted evidence for that published revision: Lint run `34306174606`, attempt 1, job `102323220424` exceeded `30m0s`. Seven executor methods completed without a full summary; subsequent detekt/ktlint steps were skipped. This is a budget failure, not a new assertion failure or the former ENOENT. Separate release-state job `102323220161` passed 73/73 (1602.880 s), per the CLB-84 handoff.
- User authorized only local lint timeout `30 -> 120`, its exact selfcheck expectation/fixtures and this checkpoint. Release-state remains `50`; both jobs, steps, delegation, concurrency, permissions and production/security behavior are unchanged. The 120-minute cap is neither a duration forecast nor a hosted PASS guarantee.
- Local checks passed: Bash syntax, workflow YAML/capability validators (22 workflows), and the actual contract/fixture block extracted byte-for-byte to a temporary launcher (5.246 s). All 31 prior negative fixtures plus four lint-timeout cases passed; a disposable `120/50` workflow was accepted. Lint `30`, `119`, `121` and missing timeout were rejected specifically by the lint-timeout guard; changed/missing release-state timeout was rejected by its exact-50 guard. Parsed YAML comparison found only `jobs.lint.timeout-minutes` changed.
- Full executor/Linux/Gradle/IT/selfcheck suites were not rerun for this configuration delta. Prior fixture-portability and backend/release-state evidence applies only to unchanged inputs; changed contract checks above are new. No new hosted run occurred. Historical `STATUS_UNAVAILABLE` and all LIVE execution gates remain unchanged.
- Evidence: `/private/tmp/clb82-lint-timeout-9qc9q0ds/` records commands/cwd/exits, fixture diagnostics, hashes, full-index patch and final status. Only these three authorized paths remain unstaged/uncommitted. No publication, workflow/GitHub settings action, server access or provisioning was performed or authorized. Next step: pass the local diff to ChatGPT for targeted review before a separate publication decision.

## Historical CLB-84: Git integration complete; post-merge security failed

- PR #497 merged at `1605c9bc96343f4bd14b5e5a7a346f5860d696e0`, parents `18606bed36bdda2c8024b0f93f6c01d16ccabac4` and `f30e0cf500070c5f1e1d573d3cafa62ecb26e63b`, tree `fcaedfae590d5db14822cd06ee95ccbad9a53ab1`. CLB-85 preflight corroborated local main/cached origin/main/live main, one worktree, clean index/worktree and no active Git operation. Former feature branch was absent; no merge/cleanup action was repeated.
- One read-only post-merge snapshot on 2026-09-09 found all 15 push workflows completed, attempt 1: 13 success; Security Scan `34319797410` and Docker Publish `34319797408` failure. Saved CLB-84 reports identify `io.netty:netty-handler:4.2.16.Final`, `CVE-2026-75595`, CRITICAL, in JVM runtime and image. The version was already in PR #497's base; it is not attributed to the executor.
- Published image `sha256:58be9083a136de33589eeeb99be21ad8567b27f15ee8850229f44c77c8815622` failed its security gate. It is unchanged, not a deploy/recovery candidate, and is not repaired by the following local work.

## Historical CLB-85: local Netty patch verified

- Result: `CLB_85_NETTY_PATCH_LOCAL_DIFF_READY`. New bounded card; no Goal, subagents or new worktree. Branch `codex/fix-netty-cve-2026-75595`, HEAD `1605c9bc96343f4bd14b5e5a7a346f5860d696e0`, clean index and exactly three unstaged paths: version catalog, verification metadata and this checkpoint. No blocking product decision; no product requirement changes. Runtime compatibility evidence supports existing `PSL-AC-29/34/35/36`; accepted DEC-036/037 and LIVE gates remain unchanged.
- Minimal repair: Netty BOM `4.2.16.Final -> 4.2.17.Final`; 30 SHA-256 entries across 16 new Netty components, individually matched to published Maven Central `.sha256` files before trusting them. Existing metadata/configuration is preserved, strict verification remains enabled, and no dependency lock or additional exact Netty constraint needed updating. Official [Netty advisory](https://github.com/netty/netty/security/advisories/GHSA-c4c3-7fpv-j4q5) confirms affected 4.2.0–4.2.16 and fixed 4.2.17 (also 4.1.137); the [release](https://github.com/netty/netty/releases/tag/netty-4.2.17.Final) and configured Maven Central artifacts were available.
- Before/after strict `dependencyInsight` proves BOM selection, including Ktor's 4.2.6 requests resolving to 4.2.17. No group-wide force was added; tcnative retains its separate BOM version and is absent from the resolved runtime. Exact Security Scan Gradle command passed after `clean`: 7 JVM projects, 95 direct/555 transitive entries, 640 resolved-module entries and 479 artifacts. Rebuilt installDist contains 130 JARs, including 14 Netty 4.2.17 JARs with matching Maven Central hashes and embedded Maven identities; no 4.2.16 residue. Comparing old hosted and new local Java inventory finds exactly those 14 upgrades and no unrelated package changes.
- Fresh compatibility checks: `clean test detekt ktlintCheck` with strict verification/no configuration or build cache passed (119 actionable tasks, 112 executed; 1157 tests, 0 failures/errors, 1 existing disabled GuestFlowStartTest). `test -PrunIT=true` with the same strict/cache boundary passed (34 tasks, 6 executed; 1303 tests, 0 failures/errors, the same skip), including isolated PostgreSQL and the full JVM process-restart E2E. No new application behavior or uncovered dependency-contract assertion required source/test edits. Executor/root audits were not repeated; unchanged release inputs and the successful exact-merge Lint result remain historical evidence, not tests of the new runtime.
- Local Linux/arm64 candidate built with the unchanged Dockerfile: `clubs-bot:clb85-netty-4.2.17-local`, actual image ID `sha256:892bd0247014c4ce98f6e69a219e48e0af78dfbdfd2d9f9b1fbc4e64bd05d870`. Labels identify uncommitted Netty bytes and runtime patch SHA-256 `ff17ffaf07e7ac2fabfbfea46253835d12810ebe0b47124e8a2b68c15cbe894e`; this is not a clean-main image. Extracted `/opt/app` contains 130 JARs and the same 14 trusted Netty artifacts. Isolated image startup smoke passed actual HTTP `/ready` and `/health` (200), using an internal Docker network, synthetic credentials and disposable PostgreSQL; owned containers/volumes/network were removed.
- Trivy 0.69.3 was verified against official release checksums. Fresh task-local vulnerability DB v2 updated `2026-09-09T07:06:00.099209771Z` and Java DB v1 updated `2026-09-09T01:11:59.619689416Z`. JVM scan at 08:03:34 UTC and exact-ID image scan at 08:06:31 UTC each passed HIGH/CRITICAL, exit-code 1 contract with actual exit 0 and zero blocking results. Both inventories prove `netty-handler@4.2.17.Final`, `AnalyzedBy=jar`, 127 Java packages/125 coupled archives; image also covers 136 OS packages and the existing secret analyzer. CVE-2026-75595 is absent. Nonblocking counts: JVM 5 MEDIUM/3 LOW; image total 111 MEDIUM/30 LOW. No suppression, VEX, threshold, analyzer, workflow or budget change.
- Resolved verification failures are retained: sandbox Gradle-cache/Docker access required ordinary approvals; first image build hit Docker Hub TLS handshake timeout before application build, then succeeded after fetching the same frontend. Direct extraction encountered read-only image directory modes; tar extraction changed only local evidence-copy directory permissions. Temporary smoke harness first mixed a platform warning into stdout container ID, then assumed an unavailable host port on an internal network; separating stderr and using container-local HTTP probes resolved both without application changes. Local amd64 app image and new hosted CI remain unverified; old hosted PASS is not reused for dependency compatibility.
- Evidence: `/private/tmp/clb85-netty-qrkem3no/` contains trusted-source checksums, command/cwd/exit/time records, inventories/SARIF, test XML/counts, runtime/image manifests, complete final patch and hashes. Final diff/whitespace, scope, metadata preservation and local checkpoint links passed; no general documentation validator applies. Only source diff, local candidate image, build/test/scan caches and evidence remain; no unfinished mutation or test remains.
- No add/commit/push/PR/merge, workflow cancel/rerun/dispatch, publishing/signing, remote image changes, settings/secrets/pins, SSH/provider/server/real-data access, deployment, migration on shared environments, rollback/resume/recovery or global-config edits occurred. Old incident owner/revision/digest and runtime pins are unchanged. Independent host trust, protected stage configuration, pinned SSH_KNOWN_HOSTS, trusted original deployment-principal status, reviewed implementation/root pin and separate exact execution authority remain LIVE prerequisites.
- One next step: hand the local repair and evidence to ChatGPT for review before publication; stop after local verification.

## Historical CLB-86: host-trust exception documentation

- Result: `CLB_86_HOST_TRUST_DOCS_REPAIRED`. No Goal, branch, worktree or subagents. Direct user decision accepted 2026-09-09 is recorded as `DEC-038`: only `koteev-m/clubs_bot` / `stage` / `178.20.209.5:22` / `ssh-ed25519` / `SHA256:Li2AIDm9/OG8CHWQw16qhDfzbRM7E9uLNjPeKOZ9ST0` may use the retained key as its trust basis instead of a separate provider/VNC prerequisite.
- Evidence is deliberately bounded: CLB-86 had the existing local Ed25519 `known_hosts` entry; the user then supplied user-operated SSH continuity output at `2026-09-09T18:01:24Z` for `afraid-amber.ptr.network`, user `root`, with the same Ed25519 fingerprint. It is not independent provider evidence, proof of original key provenance or a fresh separate handshake-algorithm verification.
- Scope failure remains fail-closed: RSA/ECDSA, another Ed25519 key, host, port or environment, an absent key, fingerprint mismatch or changed endpoint does not inherit this exception and cannot update trust automatically. Strict checking remains mandatory; no `accept-new`, disabled checking, keyscan or fallback is accepted.
- `DEC-037` live stage policy, secret-scope checks, protected deployment principal, implementation/root pins, incident owner/revision/digest, migration evidence, pin provisioning/verification, manual status dispatch and separate one-use execution authority remain distinct LIVE gates. The decision proves neither a provisioned pin, successful status, healthy stage nor any deployment/recovery authority; root observer is not the deployment principal.
- Repair records the roadmap gate order explicitly: live `DEC-037` application and independent verification precede establishment and endpoint binding of the host-trust basis. `DEC-038` has no undefined option label; product README calls it the eighteenth accepted record and a separate operational security/governance decision.
- Local scope is only the decision register, roadmap, release runbook, this checkpoint and product-readme count/reconciliation. No code/workflow/test/configuration/known-host change, provider/browser/SSH operation, Git/GitHub mutation, secret/pin provisioning, dispatch, deployment, recovery or Google-journal write occurred.
- One next step: hand the local documentation diff to ChatGPT for review.

## Historical CLB-90 continuation: prior-status v3 local repair

- Same CLB-90 card; no Goal, subagents, branch or worktree created. Starting `main`/cached `origin/main`/HEAD: `3f8a5d978ff5d0d17f0d57736ef819376895a13c`, tree `351dcb51bce88ba5df8487aad385d9a41938e72a`, one worktree, clean index/worktree/untracked. Live main equality was established for the earlier dispatch, not reasserted by this local-only continuation.
- Historical CLB-90 dispatch: one authorized Release Status run `34437951351`, attempt 1, same implementation SHA, `workflow_dispatch` from main, created `2026-09-10T04:38:19Z`, completed/success `04:43:40Z`. Exact stage approval occurred; five-minute timer was respected. Fresh DEC-037 and DEC-038 retained fingerprint/payload hash/pin metadata checks passed; repository/stage secret counts `0/6`, overlap `0`. These observations do not grant further dispatch or runtime authority.
- Historical channel verdict `CLB_90_RELEASE_STATUS_TRUSTED`: owner/revision/digest matches yes; `checkpoint=migration_completed`, `migration_evidence=present`, `app_state=ambiguous`, `operation_result=unavailable` for requested `resume-start`, abort/resume permissions no, failure category none. Ordinary app absence and DB health remain earlier observer evidence; candidate start was not proved. Run is historical trusted observation only, not usable execution-authority prior status.
- Confirmed defect `CLB_90_PRIOR_STATUS_LOG_MASKING_DEFECT_CONFIRMED`: v2 emitted raw image digest; GitHub masked persisted log content; the consumer's literal comparison failed. The old run also lacks the required original `start/remote_failure` result. Neither issue is repaired by accepting or reconstructing v2.
- Local repair: provenance v3 removes raw digest/hash authority and computes strict boolean `exact_incident` from actual inputs against one immutable contract shared by producer, independent verifier and corrected executor. Only the exact accepted stage/tag/owner/revision/digest, original `start` and retained helper hash attest true. Other valid generic status requests remain successful with false attestation. Consumer requires authenticated producer chain/run/attempt/jobs/step, its own exact incident tuple, one strict v3 record, principal/path binding and canonical trusted `migration_completed/present/remote_failure`; no v2 fallback. Claim/replay/one-use/helper/server state logic and workflow privileges are unchanged.
- Scope: existing operational IDs `PSL-AC-29/34/35/36`; no blocking product decision, no change to product requirements or DEC-036/037/038. Allowed only local deploy/evidence code, regression tests and affected operational docs. New dispatch/approval, credentials/settings/secrets changes, server access/runtime actions and publication remain prohibited. Corrective `requested_operation=start` dispatch is blocked until the new contract is reviewed/merged and separately authorized after fresh gates.
- Completed local checks: focused `V3EvidenceTest` 9/9 PASS (16.325 s); full read-only strict suite 80 methods/322 subtests PASS, all non-pass counters zero (305.926 s); full quiesced-deployment validator, workflow YAML/capability validators (22 workflows), changed Python source compilation, operational-document links and diff whitespace PASS. Required `./gradlew test` initially could not open its sandboxed Gradle cache lock; the same command with ordinary cache access completed successfully in 1m 5s (34 tasks: 2 executed, 4 from cache, 28 up-to-date). No application-code change or fresh-all-Gradle claim.
- Final local result: `CLB_90_PRIOR_STATUS_V3_LOCAL_PATCH_READY`. Full `python3 -B scripts/tests/test_corrected_stage_release.py` completed 61/61 PASS, exit 0, 4317.182 s, without failures/errors/skips: all 52 previous tests plus 9 v3 tests. This covers actual isolated producer -> simulated persisted GitHub masking -> verifier, negative/generic attestation, strict types/version/duplicates, independent consumer tuple, run/job/source authentication, canonical trust/state/principal/path guards and unchanged authorization/claim/replay/root protections. Transport and Engine are synthetic; this is not stage execution evidence.
- Final repository boundary: same `main`/HEAD/cached `origin/main` and HEAD tree, clean index, five intended unstaged files (producer, corrected executor contract alias, tests, runbook, checkpoint), no new worktree or untracked fixture residue. No unfinished test or remote operation. Evidence logs and review patch/manifest: `/private/tmp/clb90-v3-local-evidence/`. Hosted v3 log persistence, native Linux CI and a usable v3 prior run remain unverified; no new dispatch is authorized. This local continuation performed zero dispatches, GitHub external mutations, server/runtime mutations or publication; only local code/docs, disposable test resources and standard caches changed.
- One next step: ChatGPT review of the local patch before publication.
